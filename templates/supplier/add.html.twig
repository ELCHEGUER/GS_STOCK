{# templates/model/add.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}Add Supplier{% endblock %}

{% block body %}
    <div class="container">
        <h1>Add Supplier</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <br>
        <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
